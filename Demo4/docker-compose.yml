# Docker Compose for AM (AgentManager) MCP Server
services:
  # MCP Server
  agent-manager:
    build: .
    ports:
      - "8001:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:password@postgres:5432/agent_manager
      - REDIS_URL=redis://redis:6379
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - SERVER_API_TOKEN=${SERVER_API_TOKEN:-default_token}
      - LOG_LEVEL=INFO
      - ENVIRONMENT=production
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./logs:/app/logs
      - ./projects:/app/projects
      - ./.env:/app/.env:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    networks:
      - agent-manager-network

  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_DB=agent_manager
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - agent-manager-network

  # MCP Worker Clients (10 instances)
  worker-analyst-1:
    build: .
    command: ["python", "client_worker.py", "--config", "mcp_client_config.json"]
    environment:
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FAST_MODE=false
      - WORKER_ID=analyst-1
      - WORKER_ROLE=analyst
    depends_on:
      agent-manager:
        condition: service_healthy
    volumes:
      - ./mcp_client_config.json:/app/mcp_client_config.json:ro
      - ./logs:/app/logs
      - ./projects:/app/projects
    healthcheck:
      test: ["CMD", "curl", "-f", "http://agent-manager:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - agent-manager-network

  worker-analyst-2:
    build: .
    command: ["python", "client_worker.py", "--config", "mcp_client_config.json"]
    environment:
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FAST_MODE=false
      - WORKER_ID=analyst-2
      - WORKER_ROLE=analyst
    depends_on:
      agent-manager:
        condition: service_healthy
    volumes:
      - ./mcp_client_config.json:/app/mcp_client_config.json:ro
      - ./logs:/app/logs
      - ./projects:/app/projects
    healthcheck:
      test: ["CMD", "curl", "-f", "http://agent-manager:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - agent-manager-network

  worker-writer-1:
    build: .
    command: ["python", "client_worker.py", "--config", "mcp_client_config.json"]
    environment:
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FAST_MODE=false
      - WORKER_ID=writer-1
      - WORKER_ROLE=writer
    depends_on:
      agent-manager:
        condition: service_healthy
    volumes:
      - ./mcp_client_config.json:/app/mcp_client_config.json:ro
      - ./logs:/app/logs
      - ./projects:/app/projects
    healthcheck:
      test: ["CMD", "curl", "-f", "http://agent-manager:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - agent-manager-network

  worker-writer-2:
    build: .
    command: ["python", "client_worker.py", "--config", "mcp_client_config.json"]
    environment:
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FAST_MODE=false
      - WORKER_ID=writer-2
      - WORKER_ROLE=writer
    depends_on:
      agent-manager:
        condition: service_healthy
    volumes:
      - ./mcp_client_config.json:/app/mcp_client_config.json:ro
      - ./logs:/app/logs
      - ./projects:/app/projects
    healthcheck:
      test: ["CMD", "curl", "-f", "http://agent-manager:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - agent-manager-network

  worker-researcher-1:
    build: .
    command: ["python", "client_worker.py", "--config", "mcp_client_config.json"]
    environment:
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FAST_MODE=false
      - WORKER_ID=researcher-1
      - WORKER_ROLE=researcher
    depends_on:
      agent-manager:
        condition: service_healthy
    volumes:
      - ./mcp_client_config.json:/app/mcp_client_config.json:ro
      - ./logs:/app/logs
      - ./projects:/app/projects
    healthcheck:
      test: ["CMD", "curl", "-f", "http://agent-manager:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - agent-manager-network

  worker-researcher-2:
    build: .
    command: ["python", "client_worker.py", "--config", "mcp_client_config.json"]
    environment:
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FAST_MODE=false
      - WORKER_ID=researcher-2
      - WORKER_ROLE=researcher
    depends_on:
      agent-manager:
        condition: service_healthy
    volumes:
      - ./mcp_client_config.json:/app/mcp_client_config.json:ro
      - ./logs:/app/logs
      - ./projects:/app/projects
    healthcheck:
      test: ["CMD", "curl", "-f", "http://agent-manager:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - agent-manager-network

  worker-developer-1:
    build: .
    command: ["python", "client_worker.py", "--config", "mcp_client_config.json"]
    environment:
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FAST_MODE=false
      - WORKER_ID=developer-1
      - WORKER_ROLE=developer
    depends_on:
      agent-manager:
        condition: service_healthy
    volumes:
      - ./mcp_client_config.json:/app/mcp_client_config.json:ro
      - ./logs:/app/logs
      - ./projects:/app/projects
    healthcheck:
      test: ["CMD", "curl", "-f", "http://agent-manager:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - agent-manager-network

  worker-developer-2:
    build: .
    command: ["python", "client_worker.py", "--config", "mcp_client_config.json"]
    environment:
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FAST_MODE=false
      - WORKER_ID=developer-2
      - WORKER_ROLE=developer
    depends_on:
      agent-manager:
        condition: service_healthy
    volumes:
      - ./mcp_client_config.json:/app/mcp_client_config.json:ro
      - ./logs:/app/logs
      - ./projects:/app/projects
    healthcheck:
      test: ["CMD", "curl", "-f", "http://agent-manager:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - agent-manager-network

  worker-tester-1:
    build: .
    command: ["python", "client_worker.py", "--config", "mcp_client_config.json"]
    environment:
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FAST_MODE=false
      - WORKER_ID=tester-1
      - WORKER_ROLE=tester
    depends_on:
      agent-manager:
        condition: service_healthy
    volumes:
      - ./mcp_client_config.json:/app/mcp_client_config.json:ro
      - ./logs:/app/logs
      - ./projects:/app/projects
    healthcheck:
      test: ["CMD", "curl", "-f", "http://agent-manager:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - agent-manager-network

  worker-architect-1:
    build: .
    command: ["python", "client_worker.py", "--config", "mcp_client_config.json"]
    environment:
      - LOG_LEVEL=INFO
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FAST_MODE=false
      - WORKER_ID=architect-1
      - WORKER_ROLE=architect
    depends_on:
      agent-manager:
        condition: service_healthy
    volumes:
      - ./mcp_client_config.json:/app/mcp_client_config.json:ro
      - ./logs:/app/logs
      - ./projects:/app/projects
    healthcheck:
      test: ["CMD", "curl", "-f", "http://agent-manager:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - agent-manager-network

  # Redis (optional, for future caching/queuing)
  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - agent-manager-network

volumes:
  postgres_data:
  redis_data:

networks:
  agent-manager-network:
    driver: bridge


